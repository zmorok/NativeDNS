add_executable(nativedns_tests foundation.cpp)
target_link_libraries(nativedns_tests PRIVATE NativeDNS.Core.Internal)
add_test(NAME foundation COMMAND nativedns_tests)
add_executable(abi_c_test abi.c)
target_link_libraries(abi_c_test PRIVATE NativeDNS.Core)
set_target_properties(abi_c_test PROPERTIES LINKER_LANGUAGE CXX)
if(NATIVEDNS_MODULAR)
  add_custom_command(TARGET abi_c_test POST_BUILD COMMAND ${CMAKE_COMMAND} -E copy_if_different $<TARGET_FILE:NativeDNS.Core> $<TARGET_FILE_DIR:abi_c_test>)
endif()
add_test(NAME c_abi COMMAND abi_c_test)
add_executable(config_tests config_tests.cpp)
target_link_libraries(config_tests PRIVATE NativeDNS.Core.Internal)
target_compile_definitions(config_tests PRIVATE FIXTURE_PATH="${CMAKE_CURRENT_SOURCE_DIR}/fixtures/yogadns_config.xml")
add_test(NAME config_rules_yoga COMMAND config_tests)
add_executable(dns_tests dns_tests.cpp)
target_link_libraries(dns_tests PRIVATE NativeDNS.Core.Internal)
add_test(NAME dns_wire_and_loopback COMMAND dns_tests)
set_tests_properties(dns_wire_and_loopback PROPERTIES TIMEOUT 40)
add_executable(secure_tests secure_tests.cpp)
target_link_libraries(secure_tests PRIVATE NativeDNS.Core.Internal)
target_compile_definitions(secure_tests PRIVATE FIXTURE_PATH="${CMAKE_CURRENT_SOURCE_DIR}/fixtures/yogadns_config.xml")
add_test(NAME secure_validation COMMAND secure_tests)
option(NATIVEDNS_LIVE_TESTS "Enable external secure DNS/certificate tests" OFF)
if(NATIVEDNS_LIVE_TESTS)
  add_test(NAME secure_live COMMAND secure_tests --live)
  set_tests_properties(secure_live PROPERTIES LABELS live TIMEOUT 60)
endif()
add_executable(dnscrypt_tests dnscrypt_tests.cpp)
target_link_libraries(dnscrypt_tests PRIVATE NativeDNS.Core.Internal)
add_test(NAME dnscrypt_vectors COMMAND dnscrypt_tests)
if(NATIVEDNS_LIVE_TESTS)
  add_test(NAME dnscrypt_live COMMAND dnscrypt_tests --live)
  set_tests_properties(dnscrypt_live PROPERTIES LABELS live TIMEOUT 30)
endif()
add_executable(ipc_tests ipc_tests.cpp)
target_link_libraries(ipc_tests PRIVATE NativeDNS.Core.Internal)
add_test(NAME cross_platform_ipc COMMAND ipc_tests)
set_tests_properties(cross_platform_ipc PROPERTIES TIMEOUT 30)
add_executable(lifecycle_tests lifecycle_tests.cpp)
target_link_libraries(lifecycle_tests PRIVATE NativeDNS.Core.Internal)
add_test(NAME lifecycle_instance_lock COMMAND lifecycle_tests)
if(WIN32)
  add_executable(windivert_tests windivert_tests.cpp)
  target_link_libraries(windivert_tests PRIVATE NativeDNS.Core.Internal)
  add_test(NAME windivert_packet_rewrite COMMAND windivert_tests)
endif()

if(WIN32)
  foreach(target
      nativedns_tests
      abi_c_test
      config_tests
      dns_tests
      secure_tests
      dnscrypt_tests
      ipc_tests
      lifecycle_tests
      windivert_tests)
    nativedns_copy_sodium_runtime(${target})
  endforeach()
endif()
